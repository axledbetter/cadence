## Summary

<!-- What changed and why. 1-3 bullets. -->

## Test plan

<!-- - [ ] checkbox list of how you verified this works -->

## Changeset

Run `npx changeset` to add a release note describing this PR's
user-visible change. PRs without a changeset will fail the
`Changeset check` CI gate (unless they touch only docs/tests/CI).

If this PR is internal-only (refactor, test-only, docs, CI), the
changeset is not required — the `Changeset check` gate exempts those
paths via its filter.
